# 制造业设备管理系统 — Prompt Pack

## 系统概述

面向大型工厂/集团的设备全生命周期数字化管理平台，采用 React + Node.js 技术栈，支持混合部署。

## 模块 Prompt 清单

### PM-01 设备台账管理页面

为产品「制造业设备管理系统」的「设备台账管理」这个页面，生成一张 Web 界面草样（UI mockup）。该页面要能体现：当用户在设备台账页面进行检索时，系统应在 2 秒内返回匹配的设备列表，支持按工厂、车间、产线、设备类型、设备状态等多维度筛选。设计要点：设备实体包含设备编号、名称、型号、制造商、所属工厂/车间/产线、安装位置、设备类型、当前状态、技术参数、BOM 结构。采用表格+卡片视图切换，高级筛选面板，设备详情抽屉，批量导入/导出功能，支持二维码扫描快速定位设备。要求：画出真实的页面布局（顶部导航 + 左侧菜单 + 主操作区含设备列表表格 + 右侧筛选面板），中文占位文案、只示意不写真实数据、右上角明显标注 PREVIEW。

### PM-02 设备生命周期管理页面

为产品「制造业设备管理系统」的「设备生命周期管理」这个页面，生成一张 Web 界面草样（UI mockup）。该页面要能体现：当设备状态发生变更（采购→安装→调试→运行→维护→报废）时，系统应自动记录变更时间、操作人和变更原因，并在生命周期时间轴上可视化展示。设计要点：定义设备状态枚举和合法转换规则（状态机），每次状态变更写入 lifecycle_events 表，前端以时间轴组件展示完整生命周期。要求：画出真实的页面布局（顶部导航 + 设备详情区域 + 生命周期时间轴 + 状态流转图），中文占位文案、只示意不写真实数据、右上角明显标注 PREVIEW。

### PM-03 点巡检管理页面

为产品「制造业设备管理系统」的「点巡检管理」这个页面，生成一张 Web 界面草样（UI mockup）。该页面要能体现：当巡检人员通过移动端打开巡检任务时，系统应展示巡检路线和检查项清单，若发现异常则应支持拍照上传并实时推送告警通知给维修团队。设计要点：巡检计划支持按周期、按设备组、按路线配置。巡检项支持数值录入、是/否判断、文字备注、拍照上传。移动端采用 PWA 方案，支持离线巡检。异常发现自动创建关联工单。要求：画出真实的页面布局（移动端竖屏布局，顶部任务信息 + 巡检路线地图 + 检查项列表 + 底部操作按钮），中文占位文案、只示意不写真实数据、右上角明显标注 PREVIEW。

### PM-04 故障工单管理页面

为产品「制造业设备管理系统」的「故障工单管理」这个页面，生成一张 Web 界面草样（UI mockup）。该页面要能体现：当故障工单被创建时，系统应根据设备类型和故障等级自动派单给对应维修工程师，若工单在规定时间内未处理则应自动升级给上级主管。设计要点：工单状态：待派单→已派单→处理中→待验收→已关闭。派单规则基于设备类型→维修技能匹配矩阵。升级规则：P1 故障 30 分钟未响应升级。支持工单模板和快速创建。要求：画出真实的页面布局（顶部导航 + 工单看板视图（待派单/已派单/处理中/待验收/已关闭列） + 工单详情侧栏），中文占位文案、只示意不写真实数据、右上角明显标注 PREVIEW。

### PM-05 IoT 数据采集页面

为产品「制造业设备管理系统」的「IoT 数据采集」这个页面，生成一张 Web 界面草样（UI mockup）。该页面要能体现：当 IoT 网关接入设备传感器时，系统应支持通过 Modbus/OPC-UA/MQTT 协议采集实时数据，数据延迟不超过 5 秒，并存储到时序数据库供后续分析。设计要点：协议适配器模块化设计，支持 Modbus TCP/RTU、OPC-UA、MQTT 三种协议。数据采集服务基于 Node.js + MQTT.js，支持 ≥ 1000 并发数据点。时序数据库选型 TDengine。要求：画出真实的页面布局（顶部导航 + 设备接入配置面板 + 实时数据流监控 + 协议状态仪表盘），中文占位文案、只示意不写真实数据、右上角明显标注 PREVIEW。

### PM-06 OEE/MTTR/MTBF 分析页面

为产品「制造业设备管理系统」的「OEE/MTTR/MTBF 分析」这个页面，生成一张 Web 界面草样（UI mockup）。该页面要能体现：当用户打开设备效率分析页面时，系统应自动计算并展示选定时间范围内的 OEE、MTTR、MTBF 指标，支持按设备/产线/工厂维度和日/周/月时间粒度切换。设计要点：OEE = 可用率 × 性能率 × 质量率。MTTR = 总维修时间 / 故障次数。MTBF = 总运行时间 / 故障次数。前端用 ECharts 展示趋势图、对比图、排名图。要求：画出真实的页面布局（顶部导航 + KPI 卡片区（OEE/MTTR/MTBF 大数字） + 维度/时间选择器 + 趋势图 + 设备排名列表），中文占位文案、只示意不写真实数据、右上角明显标注 PREVIEW。

### PM-07 备件管理页面

为产品「制造业设备管理系统」的「备件管理」这个页面，生成一张 Web 界面草样（UI mockup）。该页面要能体现：当备件库存数量低于预设的安全阈值时，系统应自动生成采购建议通知并推送给采购负责人，同时更新备件需求看板。设计要点：备件实体包含编号、名称、规格、适用设备型号、当前库存、安全库存、单价、供应商。出入库流程：领用申请→审批→出库→使用记录→工单关联。预警规则：当前库存 < 安全库存 → 自动触发采购建议。要求：画出真实的页面布局（顶部导航 + 备件列表表格 + 库存预警看板 + 出入库操作面板），中文占位文案、只示意不写真实数据、右上角明显标注 PREVIEW。

### PM-08 大屏管理页面

为产品「制造业设备管理系统」的「大屏管理」这个页面，生成一张 Web 界面草样（UI mockup）。该页面要能体现：当车间大屏打开设备监控看板时，系统应以不超过 10 秒的刷新频率实时展示关键设备运行状态、OEE 指标和告警信息。设计要点：大屏采用全屏 Web 页面（React + ECharts），支持多种看板模板：设备总览、产线 OEE、告警滚动、能耗监控。实时数据通过 WebSocket 推送。要求：画出真实的页面布局（深色背景大屏风格，顶部标题栏 + 左侧设备状态总览 + 中间 OEE 仪表盘 + 右侧告警滚动列表 + 底部产线状态条），中文占位文案、只示意不写真实数据、右上角明显标注 PREVIEW。

### PM-09 AI 辅助决策页面

为产品「制造业设备管理系统」的「AI 辅助决策」这个页面，生成一张 Web 界面草样（UI mockup）。该页面要能体现：当系统积累足够的设备运行和故障历史数据时，AI 模块应能基于这些数据提供故障预测预警和维护建议，故障预测准确率不低于 80%。设计要点：技术方案包括时序异常检测（Isolation Forest/LSTM）+ 故障分类（XGBoost）+ 维护推荐（规则引擎 + 协同过滤）。推理服务 Python FastAPI，REST API 对外暴露。要求：画出真实的页面布局（顶部导航 + 故障预测看板（设备风险列表 + 置信度） + 维护建议卡片 + 预测趋势图 + 模型性能指标），中文占位文案、只示意不写真实数据、右上角明显标注 PREVIEW。

### PM-10 权限管理页面

为产品「制造业设备管理系统」的「权限管理」这个页面，生成一张 Web 界面草样（UI mockup）。该页面要能体现：当系统管理员配置角色和权限时，系统应支持按角色（管理员、维修工程师、操作员、访客）和数据范围（工厂/车间级别）进行权限控制，且所有用户操作都应记录审计日志。设计要点：RBAC 模型：用户→角色→权限（功能权限+数据权限）。预置角色：系统管理员、工厂管理员、维修工程师、操作员、访客。数据权限按组织树隔离。要求：画出真实的页面布局（顶部导航 + 左侧角色列表 + 中间权限配置矩阵 + 右侧数据权限树 + 底部审计日志表格），中文占位文案、只示意不写真实数据、右上角明显标注 PREVIEW。
