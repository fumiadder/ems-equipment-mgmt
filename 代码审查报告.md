# EMS 设备管理系统 - 代码审查报告

**审查日期**: 2026-06-12
**审查范围**: /workspace/whybuddy_equipment_mgmt/ 全项目
**审查依据**: 基于前三个 Agent 的测试报告

---

## 一、P0 问题修复详情

### 1. JWT 密钥硬编码 [已修复]

**问题描述**: `server/src/middleware/auth.js` 中 JWT_SECRET 硬编码为 `'ems-server-secret-key-2024'`，存在严重安全隐患。

**修复方案**:
- 密钥改为从环境变量 `process.env.JWT_SECRET` 读取
- 保留开发环境默认值 `'ems-server-secret-key-2024-dev-only'`
- 在 `auth.js` 顶部添加 `require('dotenv').config()`

**修改文件**:
- `/workspace/whybuddy_equipment_mgmt/server/src/middleware/auth.js`

---

### 2. 所有 API 未启用认证 [已修复]

**问题描述**: `auth` 中间件已定义但未被任何路由使用，所有业务接口均可匿名访问。

**修复方案**:
- 在 `server/src/routes/index.js` 中为所有业务路由添加 `auth` 中间件
- `/api/v1/auth` 保持公开（登录/注册）
- 其余路由（equipment、work-orders、spare-parts、inspection、organizations、users、dashboard）均需认证

**修改文件**:
- `/workspace/whybuddy_equipment_mgmt/server/src/routes/index.js`

---

### 3. npm start 会清空数据库 [已修复]

**问题描述**: `package.json` 的 `start` 脚本为 `"node src/seed/seed.js && node src/app.js"`，每次启动都会执行 `seed.js` 并 `force: true` 重建数据库。

**修复方案**:
- `start` 脚本改为仅启动服务：`"node src/app.js"`
- 新增 `seed` 脚本：`"node src/seed/seed.js"`
- 新增 `dev` 脚本（开发环境一键初始化+启动）：`"node src/seed/seed.js && node src/app.js"`

**修改文件**:
- `/workspace/whybuddy_equipment_mgmt/server/package.json`

---

### 4. 缺少登录/认证 API [已修复]

**问题描述**: 系统无 `/api/v1/auth/login` 和 `/api/v1/auth/register` 接口。

**修复方案**:
- 新建 `server/src/controllers/auth.js`，实现：
  - `POST /api/v1/auth/login` - 用户登录（校验密码、生成 JWT）
  - `POST /api/v1/auth/register` - 用户注册（密码哈希、唯一性校验）
  - `GET /api/v1/auth/me` - 获取当前用户信息（需认证）
- 新建 `server/src/routes/auth.js` 挂载上述接口
- 登录接口返回 `token` 和 `user` 对象

**新增文件**:
- `/workspace/whybuddy_equipment_mgmt/server/src/controllers/auth.js`
- `/workspace/whybuddy_equipment_mgmt/server/src/routes/auth.js`

---

### 5. 无 .env 配置 [已修复]

**问题描述**: 项目无环境变量配置，所有配置硬编码。

**修复方案**:
- 安装 `dotenv` 依赖
- 新建 `server/.env`（开发环境配置）
- 新建 `server/.env.example`（模板文件，供生产环境参考）
- `app.js` 和 `auth.js` 顶部加载 `dotenv`

**新增/修改文件**:
- `/workspace/whybuddy_equipment_mgmt/server/.env`
- `/workspace/whybuddy_equipment_mgmt/server/.env.example`
- `/workspace/whybuddy_equipment_mgmt/server/src/app.js`
- `/workspace/whybuddy_equipment_mgmt/server/src/middleware/auth.js`

---

## 二、P1 问题修复详情

### 6. 添加请求日志 [已修复]

**问题描述**: 后端无访问日志，无法追踪请求和排查问题。

**修复方案**:
- 安装 `morgan` 依赖
- 在 `app.js` 中添加 `app.use(morgan('combined'))`

**修改文件**:
- `/workspace/whybuddy_equipment_mgmt/server/package.json`
- `/workspace/whybuddy_equipment_mgmt/server/src/app.js`

---

### 7. 添加输入校验 [已修复]

**问题描述**: 所有接口均未对请求参数进行校验，存在 SQL 注入、XSS 等风险。

**修复方案**:
- 安装 `express-validator` 依赖
- 新建 `server/src/middleware/validate.js`，定义通用校验规则：
  - `loginValidation`: 用户名/密码非空校验
  - `registerValidation`: 用户名、密码、姓名、邮箱、手机号格式校验
  - `equipmentValidation`: 设备编码/名称/类型非空校验
  - `idParamValidation`: ID 正整数校验
- 在 `auth.js` 和 `equipment.js` 路由中应用校验中间件

**新增/修改文件**:
- `/workspace/whybuddy_equipment_mgmt/server/src/middleware/validate.js`
- `/workspace/whybuddy_equipment_mgmt/server/src/routes/auth.js`
- `/workspace/whybuddy_equipment_mgmt/server/src/routes/equipment.js`

---

### 8. 修复 CORS 配置 [已修复]

**问题描述**: `app.use(cors())` 允许所有来源，存在 CSRF 风险。

**修复方案**:
- CORS 配置改为从环境变量 `CORS_ORIGIN` 读取
- 支持多来源（逗号分隔）
- 默认开发环境允许 `http://localhost:3000`
- 限制允许的 Methods 和 Headers

**修改文件**:
- `/workspace/whybuddy_equipment_mgmt/server/src/app.js`
- `/workspace/whybuddy_equipment_mgmt/server/.env`
- `/workspace/whybuddy_equipment_mgmt/server/.env.example`

---

### 9. 添加安全响应头 [已修复]

**问题描述**: 缺少安全响应头（X-Frame-Options、XSS-Protection 等）。

**修复方案**:
- 安装 `helmet` 依赖
- 在 `app.js` 中添加 `app.use(helmet())`

**修改文件**:
- `/workspace/whybuddy_equipment_mgmt/server/package.json`
- `/workspace/whybuddy_equipment_mgmt/server/src/app.js`

---

### 10. 修复前端路由 [已修复]

**问题描述**: 前端无 `/login` 路由，401 重定向后白屏。

**修复方案**:
- 新建 `client/src/pages/Login/index.tsx` - 登录页面（Ant Design Form + axios）
- 新建 `client/src/pages/Login/Login.css` - 登录页面样式
- 修改 `client/src/App.tsx`:
  - 添加 `/login` 公开路由
  - 根路由 `/` 增加认证守卫（无 token 则跳转 `/login`）
  - 登录成功后存储 token 到 localStorage 并跳转 Dashboard

**新增/修改文件**:
- `/workspace/whybuddy_equipment_mgmt/client/src/pages/Login/index.tsx`
- `/workspace/whybuddy_equipment_mgmt/client/src/pages/Login/Login.css`
- `/workspace/whybuddy_equipment_mgmt/client/src/App.tsx`

---

### 11. 修复类型不匹配 [已修复]

**问题描述**: `@types/react` v19 与 `react` v18 版本不匹配，可能导致 TypeScript 编译错误。

**修复方案**:
- 将 `@types/react` 从 `^19.2.16` 降级为 `^18.2.0`
- 将 `@types/react-dom` 从 `^19.2.3` 降级为 `^18.2.0`

**修改文件**:
- `/workspace/whybuddy_equipment_mgmt/client/package.json`

---

### 12. 添加全局错误处理 [已修复]

**问题描述**: 错误处理中间件未覆盖 JWT 错误和外键约束错误。

**修复方案**:
- 在 `server/src/middleware/errorHandler.js` 中新增：
  - `SequelizeForeignKeyConstraintError` 处理
  - `JsonWebTokenError` 处理
  - `TokenExpiredError` 处理

**修改文件**:
- `/workspace/whybuddy_equipment_mgmt/server/src/middleware/errorHandler.js`

---

## 三、验证结果

### 后端验证

| 测试项 | 命令 | 结果 |
|--------|------|------|
| 健康检查 | `curl /api/health` | 通过，返回 `{code:0, data:{status:'ok'}}` |
| 未认证访问设备 | `curl /api/v1/equipment` | 通过，返回 `401 未提供认证令牌` |
| 用户登录 | `POST /api/v1/auth/login` | 通过，返回 JWT token 和用户信息 |
| 用户注册 | `POST /api/v1/auth/register` | 通过，返回 JWT token 和用户信息 |
| 登录校验 | `POST /api/v1/auth/login` (空参数) | 通过，返回 `400 用户名不能为空; 密码不能为空` |
| 认证访问设备 | `GET /api/v1/equipment` (带 Bearer Token) | 通过，返回设备列表 |

### 前端验证

| 测试项 | 结果 |
|--------|------|
| `npm install` | 通过，无依赖冲突 |
| `npm run build` | 通过，生成 dist 目录，无编译错误 |

---

## 四、修改文件清单

### 后端修改

1. `server/package.json` - 修复 start 脚本，新增 dotenv、helmet、morgan、express-validator 依赖
2. `server/src/app.js` - 添加 dotenv、helmet、morgan、CORS 限制
3. `server/src/middleware/auth.js` - JWT 密钥改为环境变量读取
4. `server/src/middleware/errorHandler.js` - 新增 JWT 和外键错误处理
5. `server/src/middleware/validate.js` - 新增（输入校验中间件）
6. `server/src/routes/index.js` - 所有业务路由启用 auth 中间件
7. `server/src/routes/auth.js` - 新增（登录/注册路由）
8. `server/src/routes/equipment.js` - 添加设备校验规则
9. `server/src/controllers/auth.js` - 新增（登录/注册/获取用户信息控制器）
10. `server/.env` - 新增（开发环境变量）
11. `server/.env.example` - 新增（环境变量模板）

### 前端修改

1. `client/package.json` - 修复 @types/react 和 @types/react-dom 版本
2. `client/src/App.tsx` - 添加 /login 路由和认证守卫
3. `client/src/pages/Login/index.tsx` - 新增（登录页面）
4. `client/src/pages/Login/Login.css` - 新增（登录页面样式）

---

## 五、遗留建议

1. **生产环境部署前**：务必修改 `.env` 中的 `JWT_SECRET` 为强随机字符串
2. **HTTPS**: 生产环境必须启用 HTTPS，否则 JWT 存在被截获风险
3. **密码策略**: 当前仅校验长度 >= 6，建议增加复杂度要求
4. **Rate Limiting**: 建议为登录接口添加速率限制，防止暴力破解
5. **前端 Axios 拦截器**: 建议统一配置 axios 拦截器，自动添加 Authorization Header 和处理 401 跳转
